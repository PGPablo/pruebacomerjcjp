// constantes
const dataInicial = {
    array: []
}

// types
const GET_ACTIVIDADES_SUCCESS = 'GET_ACTIVIDADES_SUCCESS'

// reducer
export default function reducer(state = dataInicial, action){

}

// actions
export const obtenerActividadesAction = () => async (dispatch) => {

}